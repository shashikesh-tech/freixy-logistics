const config = {
  theme: {
    extend: {
      fontFamily: {
        outfit: "var(--font-outfit)",
        poppins: "var(--font-poppins)",
      },
    },
  },
  plugins: ["@tailwindcss/postcss"],
};

export default config;
